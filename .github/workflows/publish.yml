name: Build and Release

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    name: Build (${{ matrix.target }})
    runs-on: ${{ matrix.os }}

    strategy:
      fail-fast: false
      matrix:
        include:
          # Linux (native)
          - os: ubuntu-22.04
            target: x86_64-unknown-linux-gnu
            use-cross: false

          # Linux ARM (cross)
          - os: ubuntu-22.04
            target: aarch64-unknown-linux-gnu
            use-cross: true

          # Windows
          - os: windows-latest
            target: x86_64-pc-windows-msvc
            use-cross: false

          # macOS Intel
          - os: macos-latest
            target: x86_64-apple-darwin
            use-cross: false

          # macOS Apple Silicon
          - os: macos-latest
            target: aarch64-apple-darwin
            use-cross: false

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # ---------------------------
      # Install shaderc dependencies
      # ---------------------------

      - name: Install shaderc (Linux)
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            build-essential \
            cmake \
            ninja-build \
            pkg-config \
            libshaderc-dev

      - name: Install shaderc (macOS)
        if: runner.os == 'macOS'
        run: |
          brew update
          brew install shaderc

      - name: Install shaderc (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          choco install shaderc -y

      # ---------------------------
      # Rust toolchain
      # ---------------------------

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      # ---------------------------
      # Cross (for ARM Linux)
      # ---------------------------

      - name: Install cross
        if: matrix.use-cross == true
        run: cargo install cross

      # ---------------------------
      # Build
      # ---------------------------

      - name: Build
        shell: bash
        run: |
          if [ "${{ matrix.use-cross }}" = "true" ]; then
            cross build --release --target ${{ matrix.target }}
          else
            cargo build --release --target ${{ matrix.target }}
          fi

      # ---------------------------
      # Prepare artifact
      # ---------------------------

      - name: Package binary
        id: package
        shell: bash
        run: |
          APP_NAME=$(basename $(pwd))

          if [[ "${{ matrix.target }}" == *"windows"* ]]; then
            BIN_NAME="${APP_NAME}.exe"
          else
            BIN_NAME="${APP_NAME}"
          fi

          mkdir -p dist

          cp target/${{ matrix.target }}/release/$BIN_NAME dist/

          # Optional: strip (ignore errors on unsupported platforms)
          strip dist/$BIN_NAME || true

          ARCHIVE_NAME="${APP_NAME}-${{ matrix.target }}"

          if [[ "${{ matrix.target }}" == *"windows"* ]]; then
            zip -j dist/${ARCHIVE_NAME}.zip dist/$BIN_NAME
          else
            tar -czf dist/${ARCHIVE_NAME}.tar.gz -C dist $BIN_NAME
          fi

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.target }}
          path: dist/*

  release:
    name: Release
    needs: build
    runs-on: ubuntu-latest

    steps:
      - name: Download artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: artifacts/**/*
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}